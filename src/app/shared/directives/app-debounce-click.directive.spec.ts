import { AppDebounceClickDirective } from './app-debounce-click.directive';

describe('AppDebounceClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDebounceClickDirective();
    expect(directive).toBeTruthy();
  });
});
